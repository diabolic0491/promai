# PromAI

PromAI — специализированное web-приложение для предприятий Республики Беларусь.

## Цель MVP

- ведение карточек контрагентов;
- загрузка и хранение договоров;
- предварительный ИИ-анализ договорных рисков;
- дальнейшая генерация договоров по утверждённым шаблонам.

## Статус

Backend MVP: контрагенты, договоры, ТЗ, DOCX-генерация,
асинхронный анализ договоров и ролевая модель доступа.

## Структура проекта

- `backend` — серверная часть и API;
- `frontend` — пользовательский интерфейс;
- `infrastructure` — конфигурация развёртывания;
- `docs` — техническая документация.

## Безопасность

В репозиторий запрещено добавлять:

- реальные договоры;
- персональные данные;
- пароли;
- API-ключи;
- сертификаты ЭЦП;
- файлы `.env`.

## Роли и доступ

В MVP используются только две роли:

- `admin` — полный рабочий доступ, управление пользователями,
  профилем организации и шаблонами документов;
- `manager` — работа с контрагентами, договорами и ТЗ, просмотр
  и использование активных шаблонов.

Роль наблюдателя в MVP отсутствует.

Маршруты `/health` и `/` публичны. Остальные рабочие маршруты
требуют JWT access-токен. Аутентификация доступна через:

- `POST /auth/login`;
- `POST /auth/refresh`;
- `POST /auth/logout`;
- `GET /auth/me`.

Управление пользователями выполняется администратором через
`/users`.

Менеджер получает в списке и по идентификатору только активные,
неархивные шаблоны. Администратор может просматривать архив,
включать, выключать, архивировать и восстанавливать шаблоны.
Исходный DOCX доступен через:

```text
GET /document-templates/{template_id}/download
```

## Списки и пагинация

Списки контрагентов, договоров, ТЗ, шаблонов, пользователей,
версий договоров и запусков анализа возвращаются в едином
формате:

```json
{
  "items": [],
  "total": 0,
  "limit": 20,
  "offset": 0
}
```

Для договоров и ТЗ доступен параметр `search`. В элементах этих
списков сразу возвращаются названия контрагента и шаблона, чтобы
frontend не выполнял отдельный запрос для каждой строки.
Внутренние пути файлов хранилища в публичные DTO не включаются.

## Версии документов договоров

Каждая успешная генерация DOCX сохраняется как отдельная
неизменяемая версия. Для версии фиксируются шаблон, снимок
исходных данных, автор, размер файла и контрольная сумма SHA-256.

- `GET /contracts/{contract_id}/versions` — список версий;
- `GET /contracts/{contract_id}/versions/{version_number}/download`
  — скачивание выбранной версии;
- `GET /contracts/{contract_id}/download` — скачивание последней
  версии.

## Локальный анализ договоров

Backend использует OpenAI-совместимый API локального Ollama.
Перед запуском анализа установите модель и запустите Ollama:

```bash
ollama pull qwen3.5:4b
ollama serve
```

Параметры подключения уже приведены в `.env.example`. Результат
модели принимается только по строгой JSON Schema, а каждая цитата
повторно проверяется backend по исходному тексту документа.
Перед сохранением замечания проходят консервативную семантическую
проверку: недоказуемые выводы об отсутствии условий, ссылки на
непереданные нормативные источники, нейтральные условия и
сравнения без двух полных разных цитат отбрасываются. Числа в
выводе сверяются с цитатами, простые сравнения процентов — с
арифметикой, субъективные оценки — с наличием основания. Выводы
с одинаковыми доказательствами дедуплицируются. Фильтр также
отбрасывает выводы, прямо опровергнутые собственной цитатой, и
понижает неподтверждённые уровни high/critical до medium. Разные
сроки последовательных этапов, например поставки и начинающихся
после неё работ, не считаются противоречием.
Доказательные блоки отправляются последовательными пакетами.
В запросах к модели длинные внутренние идентификаторы блоков
заменяются короткими алиасами, а после ответа backend восстанавливает
исходные идентификаторы до проверки цитат. Это уменьшает размер
контекста и количество пакетов без изменения сохраняемых доказательств.
Параметр `CONTRACT_ANALYSIS_MAX_OUTPUT_TOKENS` ограничивает длину
одного ответа модели; значение по умолчанию — `1600`.
Одновременно во всём экземпляре PromAI выполняется не более одного
анализа договора.

Запуск анализа является асинхронным:

1. `POST /contracts/{contract_id}/versions/{version_number}/analyses`
   возвращает `202 Accepted` и запись со статусом `running`;
2. frontend опрашивает
   `GET /contracts/{contract_id}/versions/{version_number}/analyses/{analysis_id}`;
3. итоговый статус — `completed` или `failed`.

Если backend был перезапущен во время анализа, оставшаяся запись
`running` при следующем старте переводится в `failed` с кодом
`analysis_interrupted`.

## Первичная настройка RBAC

Сгенерируйте секрет и добавьте его в `.env`:

```bash
openssl rand -hex 32
```

Значение должно быть записано как `AUTH_SECRET_KEY`. Затем
пересоберите backend, примените миграцию и создайте первого
администратора:

```bash
docker compose up -d --build backend
docker compose exec backend python -m alembic upgrade head
docker compose exec backend \
  python -m app.scripts.create_admin --username admin
```

Пароль запрашивается интерактивно и не передаётся в аргументах
командной строки. Команда первоначального создания работает,
только пока таблица пользователей пуста.

Dockerfile запускает production-команду Uvicorn без `--reload`.
Для локальной разработки `docker-compose.yml` явно включает
автоперезагрузку backend.
